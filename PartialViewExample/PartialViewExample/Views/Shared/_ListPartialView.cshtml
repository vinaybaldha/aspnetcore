@model ListModel
@if(Model != null)
{
    <div class="list-container">

        <h3>@Model.ListTitle</h3>
        <ul class="list">
            @foreach (string city in Model.List)
            {
                <li>@city</li>
            }
        </ul>
    </div>
}
